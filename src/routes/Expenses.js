import React from "react";
import Header from "../Header";

const Expenses = () => {
    return (
        <div>
            <Header />
            <p>Expenses</p>
        </div>
    );
};

export default Expenses;
